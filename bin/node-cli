#!/usr/bin/env node
const { print } = require('gluegun');

// run the CLI with the current process arguments
require('../src/cli')
  .run(process.argv)
  .catch((err) => {
    print.error(err.message);
    process.exit(1);
  });
