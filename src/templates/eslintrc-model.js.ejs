module.exports = {
  env: {
    <% if (props.cjs) { %>commonjs: true,<% } else { %>commonjs: false,<% } %>
    es2021: true,
    node: true,
    jest: true,
  },
  extends: [
    'eslint:recommended',
    <% if (props.ts) { %>'plugin:@typescript-eslint/recommended',<% } %>
    'prettier',
  ],
  <% if (props.ts) { %>parser: '@typescript-eslint/parser',<% } else { %>parser: null,<% } %>
  parserOptions: {
    ecmaVersion: 'latest',
    <% if (!props.cjs) { %>sourceType: 'module',<% } else { %>sourceType: 'script',<% } %>
  },
  <% if (props.ts) { %>plugins: ['@typescript-eslint'],<% } else { %>plugins: [],<% } %>
  rules: {},
};
