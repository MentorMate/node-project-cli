module.exports = {
  env: {
    <% if (props.cjs) { %>commonjs: true,<% } else { %>commonjs: false,<% } %>
    es2021: true,
    node: true,
  },
  "extends": ['google', "plugin:prettier/recommended"],
  parser: '@typescript-eslint/parser',
  parserOptions: {
    ecmaVersion: 'latest',
    <% if (!props.cjs) { %>"sourceType": 'module',<% } else { %>"sourceType": 'script',<% } %>
  },
  "plugins": ['@typescript-eslint'],
  "rules": {},
};

